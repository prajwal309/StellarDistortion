#include<stdio.h>
const int Num = 100;

float PolytropicPressure(){
   //Define the pressure as the function of the radius in terms of polytropic equation
   float Mass = 1.0e30;   //Pressure in terms of Pascal
   float Radius = 1.0e7;
   return 0;
}
